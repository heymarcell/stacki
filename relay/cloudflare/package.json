{
  "name": "stacki-secure-relay-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "Stacki Secure Share relay \u2014 Cloudflare Worker with one SQLite Durable Object per room.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.1",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.0"
  }
}
